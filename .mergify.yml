merge_protections:
  - name: Protected path must not change
    if:
      - title ~= ^H1_PR_FILES_
    success_conditions:
      - -files = zzzz_security_target_3001.txt

merge_protections_settings:
  reporting_method: check-runs
  post_comment: false
  auto_merge_conditions:
    - title = H1_PR_FILES_3001_ATTACK
